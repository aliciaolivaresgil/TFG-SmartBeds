<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartbeds">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BedAsignUsersActivity"></activity>
        <activity android:name=".BedChartsActivity" />
        <activity android:name=".UserPassChangeActivity" />
        <activity android:name=".UserDelActivity" />
        <activity android:name=".UsersPassChangeActivity" />
        <activity android:name=".UserAddActivity" />
        <activity android:name=".UsersManagementActivity" />
        <activity android:name=".BedsManagementActivity" />
        <activity android:name=".BedsActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>